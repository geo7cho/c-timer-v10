# 깃허브로 자동 빌드하기 (컴퓨터에 아무것도 설치하지 않는 방법)

이 방법은 사용자님 컴퓨터에 Flutter나 Android Studio를 설치하지 않고도,
무료 깃허브(GitHub) 계정 하나로 apk 파일을 자동으로 만들어주는 방법입니다.
깃허브의 서버가 대신 빌드를 해주고, 다 되면 다운로드 링크를 줍니다.

## 1. 깃허브 계정 만들기 (5분)

1. https://github.com/signup 접속
2. 이메일, 비밀번호, 사용자 이름을 입력하고 안내에 따라 가입 완료
   (신용카드 필요 없음, 완전 무료)

## 2. 새 저장소(repository) 만들기

1. 로그인 후 우측 상단 `+` 버튼 → `New repository` 클릭
2. Repository name에 `counseling-timer-app` 입력
3. `Public`과 `Private` 중 아무거나 선택해도 됩니다
   (다른 사람이 코드를 보는 게 싫으시면 `Private` 선택)
4. 아래 체크박스들은 모두 그대로 두고 `Create repository` 클릭

## 3. 파일 올리기 (git 명령어 필요 없음)

1. 방금 만든 저장소의 빈 화면에서 `uploading an existing file` 글자를 클릭
   (또는 `Add file` → `Upload files`)
2. 다운로드받은 `counseling_timer_app.zip`을 먼저 압축 풀기
3. 압축을 푼 `counseling_timer_app` 폴더 안의 내용물 전체
   (`lib`, `assets`, `.github`, `pubspec.yaml`, `README.md` 등)를
   **폴더째로** 브라우저의 업로드 화면 위로 드래그해서 놓습니다
   (크롬 브라우저 기준으로 폴더 통째로 드래그하면 안의 구조가 그대로 유지됩니다)
4. 화면 하단 `Commit changes` 버튼 클릭

> 참고: `license_server` 폴더(구글 시트/Apps Script 관련 파일),
> `android_manifest_snippet.xml`, `ios_info_plist_snippet.xml` 파일은
> 이 방식에서는 필요 없지만 같이 올려도 무방합니다.

## 4. 자동 빌드 확인하기

1. 저장소 상단 메뉴에서 `Actions` 탭 클릭
2. `Build APK`라는 이름의 작업이 자동으로 실행되고 있을 겁니다
   (노란 점 = 진행 중, 초록 체크 = 완료, 빨간 X = 실패)
3. 보통 5~10분 정도 걸립니다. 완료될 때까지 기다려주세요.

만약 빨간 X(실패)가 뜨면, 그 항목을 클릭해서 나오는 로그 내용을 캡처해서
저에게 보여주시면 원인을 봐드릴게요.

## 5. APK 다운로드

1. 초록 체크가 뜬 빌드를 클릭해서 들어갑니다
2. 화면 아래쪽 `Artifacts` 부분에 `app-release-apk`라는 항목이 있습니다
3. 클릭해서 다운로드하면 zip 파일이 받아지고, 압축을 풀면
   `app-release.apk` 파일이 나옵니다

## 6. 폰으로 옮겨서 설치하기

apk 파일을 폰으로 옮기는 방법은 여러 가지가 있습니다. 편하신 걸로 하세요.

- **이메일**: 본인 메일로 apk 파일을 첨부해서 보낸 뒤, 폰에서 메일 열어 첨부파일 다운로드
- **카카오톡 "나와의 채팅"**: PC 카카오톡에서 나에게 파일 전송 → 폰 카카오톡에서 다운로드
- **구글 드라이브 / 네이버 클라우드**: PC에서 업로드 → 폰 앱에서 다운로드
- **USB 케이블**: 폰을 PC에 연결해서 apk 파일을 폰 저장소로 복사

폰에서 apk 파일을 눌러 설치를 시도하면, 안드로이드가
"출처를 알 수 없는 앱" 경고를 띄웁니다. 이때 "설정으로 이동" 또는
"허용" 버튼을 눌러, 그 파일을 연 앱(예: 파일 관리자, 카카오톡, 크롬 등)에
대해 "이 출처 허용"을 켜주면 설치가 진행됩니다. 이 경고는 스마트폰이
낯선 곳에서 온 앱을 걸러주기 위한 정상적인 안전장치이니 걱정하지 않으셔도
됩니다.

설치 후 앱을 처음 실행하면, 마이크 권한 요청과(녹음 기능을 켠 경우)
일련번호 입력 화면이 나옵니다. 라이선스 서버(구글 시트) 설정을 아직
안 하셨다면 `lib/config.dart`의 `licenseServerUrl`이 빈 값이라 일련번호
화면 없이 바로 사용됩니다 — 나중에 판매를 시작하실 때 README의
"라이선스 설정" 부분을 참고해서 깃허브 저장소 안의 `lib/config.dart`
파일을 웹에서 직접 열어(연필 아이콘 클릭) 주소를 채우고 다시 커밋하면,
다음 Actions 빌드부터 라이선스 기능이 켜집니다.
